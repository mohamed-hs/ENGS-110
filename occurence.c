#include <stdio.h>
#include <stflib.h>

int main(){

	int* point;
	int n, i;

	printf("Enter number of elements: ");
	scanf("%d", &n);

	ptr = (int*)malloc(n * sizeof(int));

	if (ptr == NULL) exit(0);

	else{
	
	for(i = 0; i < n; ++i){
	int x;
	scanf("%d", &x);
	if(x != -1) a[i]
	}
	}


}
